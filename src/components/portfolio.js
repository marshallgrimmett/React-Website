import React, { Component } from 'react';

export default class Portfolio extends Component {
  render() {
    return (
      <div>
        <h2>My Portfolio</h2>
        <p>This Website! Built using <code>create-react-app</code>
          and set up as a full-fledged PWA!
        </p>
        <p>Change Management App (in development). Uses React and Redux.</p>
        <p>College Planner App (in development). Uses React and Redux.</p>
      </div>
    );
  }
}
